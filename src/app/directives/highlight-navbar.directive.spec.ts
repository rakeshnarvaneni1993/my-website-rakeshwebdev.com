import { HighlightNavbarDirective } from './highlight-navbar.directive';

describe('HighlightNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
